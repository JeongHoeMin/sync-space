PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=syncspace
DB_PASS=syncspace_password
DB_NAME=syncspace_dev

JWT_SECRET=super_secret_dev_key

LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# SSL 설정 (mkcert 등으로 생성한 인증서 경로를 입력하세요)
# SSL_CERT_PATH=./certs/your-cert.pem
# SSL_KEY_PATH=./certs/your-key.pem

# 앱 외부 접근 URL (업로드 파일 URL 생성에 사용)
# SSL 활성화 시: APP_URL=https://your-server-ip:3000
APP_URL=http://your-server-ip:3000
